services:
  - type: web
    name: mediascribe-backend
    env: node
    buildCommand: cd backend && npm install && pip install yt-dlp
    startCommand: cd backend && npm start
    envVars:
      - key: PORT
        value: 5000
